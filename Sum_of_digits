#include <stdio.h>
int main() {
    int n,sum=0;
    printf("Enter the number:\n");
    scanf("%d",&n);
    if(n<0){
        n=-n;
    }
    while(n>0){
        int digit=n%10;
        sum+=digit;
        n/=10;
    }
    printf("Sum of the number is: %d\n",sum);
    return 0;
}
